const data=raw.split('\n').map((x,id)=>{const[name,club,league,area]=x.split('|');return{id:String(id),name,clubs:club.split('／'),league,area}}),teams=data.flatMap(s=>s.clubs);let visited=[],visitedTeams=[];try{visited=JSON.parse(localStorage.getItem('j-stadium-visited')||'[]');visitedTeams=JSON.parse(localStorage.getItem('j-team-visited')||'[]')}catch{}let league='ALL';const $=id=>document.getElementById(id),save=()=>{localStorage.setItem('j-stadium-visited',JSON.stringify(visited));localStorage.setItem('j-team-visited',JSON.stringify(visitedTeams))};function render(){const q=$('q').value.toLowerCase(),only=$('only').checked,shown=data.filter(s=>(league==='ALL'||s.league===league)&&(!only||!visited.includes(s.id))&&(s.name+s.clubs.join('')+s.area).toLowerCase().includes(q)),pct=Math.round(visited.length/data.length*100),tp=Math.round(visitedTeams.length/teams.length*100);$('done').textContent=visited.length;$('total').textContent=data.length;$('pct').textContent=pct+'%';$('bar').style.width=pct+'%';$('teamDone').textContent=visitedTeams.length;$('teamTotal').textContent=teams.length;$('teamPct').textContent=tp+'%';$('teamBar').style.width=tp+'%';$('count').textContent=shown.length+'件';$('items').innerHTML=shown.map(s=>`<div class="stadium ${visited.includes(s.id)?'done':''}"><div class="stadium-main"><button class="visit" data-id="${s.id}" aria-label="${s.name}へ行った">${visited.includes(s.id)?'✓':''}</button><span class="info"><span class="name">${s.name}</span><span class="area">${s.area}</span></span><span class="badge ${s.league}">${s.league}</span></div><div class="teams"><span class="teams-label">行ったチーム</span>${s.clubs.map(t=>`<button class="team ${visitedTeams.includes(t)?'done':''}" data-team="${t}">${visitedTeams.includes(t)?'✓ ':''}${t}</button>`).join('')}</div></div>`).join('');document.querySelectorAll('.visit').forEach(b=>b.onclick=()=>{const id=b.dataset.id;visited=visited.includes(id)?visited.filter(x=>x!==id):[...visited,id];save();render()});document.querySelectorAll('.team').forEach(b=>b.onclick=()=>{const t=b.dataset.team;visitedTeams=visitedTeams.includes(t)?visitedTeams.filter(x=>x!==t):[...visitedTeams,t];save();render()})}document.querySelectorAll('#tabs button').forEach(b=>b.onclick=()=>{league=b.dataset.l;document.querySelectorAll('#tabs button').forEach(x=>x.classList.toggle('active',x===b));render()});$('q').oninput=render;$('only').onchange=render;render();if('serviceWorker'in navigator)navigator.serviceWorker.register('./service-worker.js');
